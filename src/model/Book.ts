class Book {
    id: number
    title: string
    author: string
    pages: number

    constructor(id: number, title: string, author: string, pages: number) {
        this.id = id
        this.title = title
        this.author = author
        this.pages = pages

    }
}

export default Book